# Build-time tooling — must never be served to the public.
# build_rotation.js regenerates assets/rotation-data.js locally; the running
# site only needs the generated output, not the script that produced it.
scripts/

# BACKLOG: built but held for the app launch (do not serve yet).
# To publish: delete this line, remove the .vercelignore guard, then re-add
# the page to the sitemap + nutrition hub link in scripts/build_nutrition.js.
compare/
